# Learning INDEX

- 001 멀티 방언 SQL 파싱 전략 — 업계 수렴 답은 "방언별 정밀 파서 + 룰 전용 얇은 IR"; MySQL 1호는 Alibaba Druid 파서(WallFilter 선례), ANTLR는 CST만 줘서 AST 층 비용 큼 [파서, ANTLR, Druid, JSQLParser, Calcite, IR, 거버넌스]
- 002 스펙 001 적대 검토 — 게이트 스펙의 본체는 룰 목록이 아니라 fail-closed 판정 계약; 우회 9축 체크리스트(OR 가지·스코프 은닉·UNION·멀티문·문 종류·오탐·alias·Raw 기본값·purpose 자가 면제) [적대 검토, fail-closed, 우회, 룰 계약]
- 003 M1 코드 적대 검증 — 계약 테스트로는 파서 "표현" 결함(백틱 보존·alias 소실)을 못 잡는다; 새 파서 어댑터마다 식별자 표현 프로브 필수, 검증자에겐 "실제 jar로 확인" 요구 [검증, 백틱, 정규화, 셀프 조인, 인스턴스 귀속, fail-closed]
- 004 디자인 산출물 분석 — 목표 상태 청사진: 컬럼 클래스×kind 5종 제약 사전→컬럼 매핑→규칙이 그 어휘만 참조(2계층 통제)+승인·실행·마스킹·권한; gap B1~B14, 사람 결정 5건(판정vs재작성, severity, 파티션, 규칙 선택 fail-closed, 승인 게이트) [디자인, 제약 카탈로그, 규칙 빌더, 승인, 마스킹, gap]
- 005 spec 002 구현 — MySQL UNIQUE는 NULL 비중복(앱 검사 이중화 필수); columnRefs를 술어 모델과 분리하니 한 visitor로 전 절 커버; DTO 전문 위임=통합 수정 0건; 검토엔 "스펙 vs 구현 코드 대조" 고정 [UNIQUE NULL, columnRefs, 위임 계약, 검토 축]
- 006 디자인 활용 실패 — 모델 우선·셸 5순위 순서는 합의됐어도 사용자 기대는 "내 디자인이 보이는 것"; 디자인 산출물엔 UI 컨버팅 선행이 기본값, 로직 없는 화면은 샘플 데이터 스텁으로라도 화면 먼저 일치 [디자인 충실도, UI 우선, 기대 관리]
- 007 디자인 컨버팅 실행 — 원본이 antd 앱이면 "이식"이라 쉬움(먼저 통독 확인); 기반 직렬+화면 병렬=충돌 0; 내 브리프의 토큰 오지시(pending blue)가 전 화면 전파 → 위임 전 검토와 토큰 교차검증 [컨버팅, 병렬 위임, 토큰, 공유 상수]
- 008 규칙 데이터화 구현 — IR 축을 판정별 분리(joinEqualities를 §6.1 top-level에 얹어 OUTER·OR 우회 위치만으로 차단); enum ordinal≠심각도(worstSeverity); 미강제는 배지 아닌 규칙 플래그(enforced); 위임은 파일 완결로 중단에 강하게 [규칙 엔진, joinEqualities, severity, enforced, 빈 순환]
- 009 승인·검토 구현 — 게이트 추가가 기존 게이트를 열 수 있다(purposeCode 유실→승인 요청에서 서버 주입으로 승격); "IR 대상 테이블"은 집합 정의를 식으로(CTE 우회); HTTP 헤더는 ASCII(actor id화가 검증도 강화); 스텁 identity는 한계 명문화가 기능 [purposeCode, 테이블 커버, actor, 검토 리셋, 재-lint]
- 010 인증·접근 권한 — 권한 도입이 판정을 약화시킬 수 있다(ArchUnit으로 고정); 풀 통합은 암묵적 불변식(자가 승인 불가)을 깬다; changeSessionId는 세션 전제 필요(첫 로그인 500); 헬퍼 시그니처 유지로 이관 비용 제거 [인증, 세션, ArchUnit, 자가 승인, 위임 브리프]
- 011 접수 검사·실행 격리 — 식별자 비교 한 곳의 norm() 누락이 금지 목록 전체를 무효화(백틱 `sleep`); "검사 불가"를 빈 목록으로 보고하면 fail-open; 2차 파싱은 판정-검사 분기(실측 57/84); 접수 규칙은 프린터 출력의 고정점이어야; 오탐 조사가 CTE 자기이름 우회를 찾음 "조용히 버림"은 fail-closed 아님(파생 UNION 스코프 소실=BLOCK 컬럼 소실); 타사 모델은 공격 축을 필터로 거부→정합성 축으로 재프레이밍 [접수 검사, norm, UNVERIFIABLE, 단일 파싱, 고정점, CTE, 스코프 은닉, 검증 채널]
- 012 재작성 엔진 — 판정·재작성이 함수는 공유해도 **순회 축**이 다르면 그 사이로 샌다(평문 유출 4경로 실측); 절 열거는 새고 불변식은 남는다(모든 쿼리 노드=스코프); 주입은 부정 문맥에서 필터를 **반전**시킨다; 검증기가 계획을 기대치로 쓰면 계획 버그에 눈이 먼다; 순번 id는 다른 파싱에서 우연히 맞는다; 오차단 완화가 fail-open을 만들었다 [재작성, 순회 축, 스코프 불변식, 극성, 검증기 독립, scopeId 난스, 마스킹 many-to-one]
- 013 모바일·시각 회귀 하네스 — 지표가 사용자 증상을 재현하는지 먼저 확인(고치기 전에 실패시켜 보기); fullPage는 내부 스크롤러 아래쪽을 못 담는다; 전역 min-width:0이 버튼을 2px로 만든다; 브레이크포인트 기준은 남는 콘텐츠 폭; 1fr은 min-content 밑으로 안 줄어 형제를 밀어낸다; 표는 접지 말고 가로 스크롤로 격리 [모바일, 시각 회귀, Playwright, 지표 설계, 브레이크포인트, 그리드]
- 014 실행 게이트·감사 도달성 — 소유권이 참조 컬럼이면 그 컬럼 수정 허용=소유권 이전(PUT 탈취); append-only 감사가 대상 행의 생사에 매달리면 통제 아님(삭제로 은닉·PREVIEW는 write-only); 상한은 상한에 맞추지 말고 의존을 끊어라(파서 65536 vs TEXT 65535, 재작성은 커진다); CREATE TABLE IF NOT EXISTS는 기존 테이블에 컬럼을 안 준다(검증 질문은 "기존 DB에서 되는가"); sql_mode 치환은 판정-실행 의미를 갈라놓음(ONLY_FULL_GROUP_BY 소실); 불리언에 "모름" 자리가 없으면 언젠가 거짓말(LIMIT 0); **수정이 새 회귀를 만든다**(합집합 권고를 따르자 ANSI_QUOTES 상속 → 판정은 리터럴·실행은 컬럼); 테스트가 통과하는 **이유**를 봐라(3회 실패: dialect 오타·기본 모드 서버·픽스처가 이미 새 모양 → 되돌려 실패 확인이 습관); 쪼갤 때 옳은 축을 골라라(설정 상한≠적용 상한); 증폭은 파서 상한과도 부딪힌다(넓혀야 할 건 SQL이 아니라 applied_json) [감사 도달성, 소유권 조인 키, 상한 결합, 멱등 DDL, sql_mode, 관측 부재, 통과 이유, 수정의 회귀, 축 선택]
- 015 감사 코드 전수 검증 설계 — 타입화만으로 안 닫힌다(리포지토리가 열려 있음, 닫힘 범위를 정확히 적어라); 검증 축은 "코드 × 진입점"(한 곳만 고정하면 다른 곳 기록 삭제가 무검출); 단정은 행의 계약 전체(정확히 1건+actor 결속) + 응답 본문 코드(현행 null도 적어라—나중 변경 목록이 된다); 도달 불가 코드는 발견이지 결함 아님(판정이 재작성과 같은 기준이면 거부 코드는 2선 방어); 주입의 한계 명시+짝은 이름 집합 비교(개수 비교는 넣고빼기를 통과); 픽스처는 저장 후 세상 바꾸기+실패 캐시+PER_CLASS; 정의 고칠 땐 사본 먼저 grep [감사 코드, 전수 검증, 굿하트, 진입점, 2선 방어, spy 한계, 픽스처, 문서 사본]
- 016 값 기반 게이트 재작성 — 사본은 세지 말고 **자유변수**를 세라(정규화 diff로 셋); 실패는 값 + 예외 번역은 `raise()` 다형성(경계에 when 없음), `kotlin.Result`는 Throwable만 담아 못 씀; **순서는 공유 단위에 넣지 마라**(진입점마다 정책이 다름) 대신 타입 사슬이 일부 재배치를 컴파일로 막음; 증거 타입은 "다음 단계가 요구하는 것"으로(Ready만으론 실행 자격 부족→Executable); 실패 타입이 **자기 분류 코드를 필드로**(주석은 컴파일 안 됨); 등급 필드 달기 전에 **타입 분할이 이미 답을 가졌는지** 보라; "아무것도 안 나간다" 쓰기 전에 응답 바디를 열어라; 이유 없는 것끼리 나쁜 시너지(미사용 import는 스캐너를 짜라—컴파일러 침묵); 안전망은 현실을 재야 감시자·변경목록 둘 다 됨 [게이트, 값 기반 실패, 다형성 번역, 증거 타입, 음의 복리, 안전망]
- 017 남의 라이브러리 재귀 길들이기 — SOE 스택 트레이스의 **반복 프레임을 세어 고리를 이름으로 특정**(트레이스는 1024에서 잘림, 비율만 볼 것); "못 고친다" 전에 **이음매를 세라**(셋 중 둘이 열려 있었다), final이면 위 이음매 비용을 재되 파서 재구현은 판정-실행 분기 위험이라 거절; 상한은 **스택 용량 아닌 도메인 필요치**(상한이 스택에 가까우면 계수기가 세기 전에 터진다 — 1000 뚫림/200 잡힘, 정상 깊이 2); **깊이와 연쇄 길이는 다른 축**(평면 체인은 파서가 반복 처리해 계수기 밖, 텍스트로 세면 버전 무관); 우리 재귀는 조치 **뒤 다시 재고** 고쳐라(자동으로 유계가 된 것이 대부분); 결과를 합치면 트램폴린(stdlib `DeepRecursiveFunction`)·담기만 하면 명시 스택·꼬리재귀는 트리에 안 됨(`tailrec`은 검산 도구), 명시 스택은 LIFO라 **오른쪽 먼저 넣어** 순서 보존; 스레드 격리의 값은 시간이 아니라 스택(풀 크기는 격리용/병렬용 구분, `cancel(true)`는 CPU 작업을 못 멈춤); 라이브러리 진입점 대체 시 **원래 계약을 바이트코드로**(EOF 검사가 보안 계약이었다) [재귀, StackOverflow, 어댑터, final, 상한 설계, 트램폴린, 스레드 격리, 계약 확인]
- 018 Kotlin 발급 권한 폐쇄 — 장치 셋의 **범위가 다르다**(`sealed`=패키지·`private` 최상위=파일·`internal`=모듈, 뭉뚱그리면 가짜 제약이 생기고 그 안의 나쁜 구조까지 정당화된다); friend 가시성이 없으면 **전용 패키지 + 파일 private 구현체**로 겹쳐 조립(같은 패키지에 협력자가 살면 처음부터 샌다); "구조로 불가능"은 **층위를 적어라**(private 구현체는 JVM에선 package-private+public 생성자라 리플렉션은 뚫는다 — 목표는 "평범한 코드가 실수로 건너뛰면 컴파일 오류"); ArchUnit `haveRawType`은 **제네릭 소거에 눈이 멀어** `List<X>`·`Box<X>?`를 통과시킨다(타입 인자 재귀 + **와일드카드 경계**까지 — 공변 선언이면 `Box<? extends X>`가 된다); 규칙이 **자기 자신을 위반**할 수 있다(클래스 리터럴=의존 → 문자열 대신 `DoNotIncludeTests`로 대상을 좁혀 컴파일러가 이름을 지키게); 순서가 정책이라 갈리면 **타입 파라미터가 무엇을 통과했는지 기억**(`Judged<out Prior : Authorized>` → `resolveMapping(Judged<ApprovalCovered>)`, 파라미터 이름은 필드명과 맞출 것); **CI 유무가 테스트냐 컴파일러냐를 바꾼다**; 감시자는 **하나하나** 되돌려 실패(세 번 했다는 안전을 주지 않는다 — 안 해 본 하나가 뚫려 있다) [발급 권한, sealed 범위, 전용 패키지, 제네릭 소거, ArchUnit, 타입 파라미터, 되돌려 실패]
