# Retrospect INDEX

- 001 MVP 구축 — 적대 검토는 스펙·코드 두 층위에 각각(잡는 결함이 다름); 위임 계약서엔 응답 스키마까지; 다음 후보: 변이문 거버넌스·relint·PG 어댑터 [MVP, 적대 검토, 위임, 다음 작업]
- 002 MVP 직후 패치 — "작은 작업"에서 루프 규율 임의 축소(무합의 스킵·참고 자산 누락·회고 없는 커밋); 스킵은 합의로만, 크기와 무관 [루프 규율, 스킵 합의, 회고, 의존성 누락]
- 003 제약 카탈로그 개편 — 검토자에게 기존 코드 대조를 시켜 스펙-코드 결함(C1) 발견; DB 제약 의미 확인을 검토 축에 추가; 다음: spec 003 규칙 데이터화 [spec 002, C1, 검토, 다음 작업]
- 004 디자인 UI 전면 컨버팅 — 피드백을 learning으로 먼저 전환 후 실행; 원본 통독→이식, 7화면 병렬 재질문 0, 실 게이트 온전; 브리프 토큰은 검토와 교차검증 [spec 003, 컨버팅, 충실도, 다음 작업]
- 005 규칙 데이터화 — 루프 6원칙 충실 이행; 적대 검토가 joins §6 우회 4건 사전 차단(수집 위치로 해결); 판정 핵심 직접·화면 위임; 시드 스크립트 자산화; 테스트 커버리지 갭 남음 [spec 004, 루프 규율, joins, 위임, 다음 작업]
- 006 승인·검토 워크플로 — 사용자의 모델 교정(승인 후 작성)이 뼈대를 바로잡음; 적대 검토가 C1(게이트 역설) 확인; 헤더 ASCII 실패 후 id 도입; 위임 ambiguity 되먹임 절차화 필요; 테스트 부채 누적 [spec 005, 모델 교정, 게이트 순서, 부채]
- 007 테스트 부채 상환 — 2라운드 누적 부채를 기능 전에 상환; 프로덕션 변경 0으로 "코드는 맞고 테스트만 없음" 확인; 동시성은 실제 스레드 경합으로 검증 [부채, 회귀, 동시성]
- 008 인증·접근 권한 — 세션 승격으로 spec 005의 "통제 아님" 해소; 위임이 내 세션 고정 버그(첫 로그인 500) 잡음; 풀 통합이 암묵 불변식 깸 [spec 007, 인증, 권한, 위임]
- 009 접수 검사·실행 격리 — M0 선행 순서가 옳았음; 브리프의 "실측하라"가 CRITICAL 4건 잡음; 오탐 축 검토가 진짜 우회를 발견; 내 불변식을 내 새 코드가 어김; 타사 검증 미완주(파이프라이닝 필요) [spec 008 M0, 적대 검토, 오탐, 불변식, 병렬 위임]
- 010 재작성 엔진 — 검증 2축 배분(공격=사내·정합성=타사)이 겹치지 않고 각각 잡음, 보안 기능은 공격 축 우선; 함수만 공유하고 순회 축을 놓침·기존 축의 fail-closed 폴백 미대조; 오차단 급수정이 fail-open; 병렬 검증의 격리 단위는 파일이 아니라 작업 공간 [spec 008 M1, 검증 배분, 순회 축, 완화의 위험, 병렬 격리]
- 011 모바일 레이아웃 — 측정을 먼저 만든 것이 부작용·오판을 자동으로 잡음, 불변 약속에 스냅샷 증거를 붙임; 반성: 첫 지표가 증상을 재현 못했고 폭만 재고 완료 보고, 스크린샷 3장만 보고 넘어감(사용자 실기기가 1차 검증자다) [spec 009, 지표 설계, 시각 검증, 전역 CSS 부작용]
- 012 실행·감사 마일스톤 — 검증 3라운드(타사 정합성 → 사내 적대 → **수정본 재적대**), 3라운드 8건 중 3건이 내 수정이 만든 결함(sql_mode 합집합→ANSI_QUOTES 상속); 반성: 내가 만든 테스트 3개가 엉뚱한 이유로 초록(되돌려 실패 확인을 루틴화), 감사를 쓰기 경로만 보고 완료 선언, 스키마 함정을 같은 파일에서 두 번(nullability); 잘된 것: 게이트 순서 선문서화·실측 요구 브리프·fail-closed 기본값 [spec 008 M2, 수정의 회귀, 통과 이유, 감사 도달성, 멱등 DDL, 브리프 설계]
- 013 게이트 재설계 검토 3R — 구현 전 문서 단계에서 23건 차단; 내 v2 수정이 PREVIEW 감사를 강등한 새 회귀; 불변식 두 줄이 서로 모순인데 두 라운드가 못 봄("불변식은 소원이 아니다"); 철회 결론은 옳고 근거 2개가 부정확; "타입으로 막는다"의 본체는 필드가 아니라 발급 권한 폐쇄; 수용 기준이 대리 변수라 굿하트에 열림 [spec 010, 설계 검토, 모순 은닉, 근거 부풀림, 생성 경로 폐쇄, 굿하트]
- 014 게이트 재설계 P0 — 감사 코드 21종 전수 검증이 3가지 사실을 처음 드러냄(도달 불가 6종·재작성 실패가 403·응답에 코드 없는 경로 9개); 반성: 굿하트 방지 조항을 내가 쓰고 내가 통과당함(코드당 시나리오 1개→execute 감사 래퍼 무보호, 그 결함은 주석에 적혀 있었다), 단정 축이 계약보다 좁음(errorCode+status만), "컴파일 시점에 닫힌다"를 또 부풀림, 정의 정정 시 사본 미확인; 사용자 평가("지적하니 따라왔다")는 칭찬이 아니라 발견 — 내 검증 채널에 **장인 축이 0개**라 설계 결함이 전 라운드 통과, 레퍼런스가 있어야 목표가 구체가 됨; 잘된 것: 되돌려 실패 3회·독립 2채널이 겹친 3건 [spec 010 P0, 굿하트, 진입점 축, 단정 설계, 근거 부풀림, 문서 사본, 장인 축, 선례 탐색]
- 015 게이트 줄기 P1 — 사본 3벌→1벌·execute 117→15줄·응답 코드 없는 경로 15→0; **안전망이 두 방향**(구조 변경엔 감시자, 계약 변경엔 변경 목록 — 현행을 그대로 고정해 둔 덕); 반성: 계획서에서 항목을 **조용히** 빠뜨림(백로그엔 순서까지 적어 놓고), 근거 부풀림 **세 번째**(규율로 안 고쳐짐→적대 브리프에 "단언을 반증하라" 상설), 불변식 문장의 절반을 흘림(I7 "만들지도 잡지도"에서 throw 누락), **음의 복리를 내가 만듦**(무해한 셋이 합쳐 감사 소실 경로); 잘된 것: 축 가른 2채널이 각각 다른 것을 잡고 겹친 2건은 실재, 검토를 그대로 따르지 않은 판단 1건 [spec 010 P1, 안전망 양방향, 항목 스킵, 근거 부풀림, 음의 복리, 채널 분리]
- 016 파서 재귀 유계 P1.5 — 사용자가 **네 번 밀었고 네 번 다 내가 틀렸다**(스택 안 쓰기·꼬리재귀·오버라이드·어댑터); 반성: v1 계획이 통째로 방어막(스펙이 준 "자원 유계" 틀 안에서만 답을 찾음), **"못 한다"를 확인 없이 단정**(부풀림과 같은 뿌리 — 할 수 있다/없다에 같은 증명 부담), 조건이 다른 것을 비교하고 "느리다" 결론 낼 뻔(O(n²) vs O(n)), 테스트가 바이트 상한 덕에 초록(네 번째지만 처음으로 스스로 잡음); 잘된 것: 조치 뒤 다시 재서 범위를 2함수로 좁힘·스택 트레이스로 고리 특정·EOF 계약을 바이트코드로 확인·재구현 거절 판단; 스펙 전제 2개가 실측으로 뒤집혀 §4.7·A7 정정 [spec 010 P1.5, 원인 제거, 확인 없는 단정, 불공정 비교, 통과 이유, 스펙 정정]
