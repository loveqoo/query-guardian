# 작업 방식 (Operating Manual)

이 저장소의 모든 작업은 아래 **6단계 루프**를 반복한다. 작업 성격에 따라
특정 단계가 불필요할 수 있다 — 이 경우 **사람과 합의 + 사유 명시** 후 스킵한다.

> **루프는 "작업이 정해질 때" 돌기 시작한다.** 할 일을 정하는 Scaffolding이 진입점이다 —
> 작업이 없으면 루프도 없다. 그래서 "다음 뭐 하지?"는 Planning이 아니라 Scaffolding이다.

> **항상 현재 단계를 명시한다.** 매 응답 머리에 `[단계 N — 이름]` 형태로
> 지금 6단계 루프 중 어디에 있는지 표기하여, 사람과 AI가 루프 위치를
> 늘 공유한다.

---

## 6단계 루프

### 1. Scaffolding — 구조 점검·정비
- 작업에 맞는 구조인지 먼저 확인한다. 변경이 필요할 때만 구조를 손댄다.
- 그 외에는 **역할별 폴더링**을 유지한다.
- **"다음 할 일 후보를 리스트하자/다음 뭐 하지?"는 이 단계다**(Planning이 아니다) — 여기서 할 일을
  정해야 구조 점검·정비를 시작할 수 있기 때문. 단, **사용자가 다음 일을 물으면 먼저 직전 작업이
  마무리·회고까지 끝났는지 확인**한다. 미진하면 **회고(Compounding)를 먼저 마친 뒤** Scaffolding으로
  넘어가 다음 작업을 논의한다.
- **컨텍스트 최적화는 이 단계에서 한다**(기능 작업의 Planning→Execution이 아니다) — 루프를 *돌리기 위한*
  구조 정비이기 때문. 자산이 무거워졌는지 점검하고, 필요하면 재정비한다. 특히 **`INDEX.md` 줄은
  "한 줄 후크"를 유지**한다(서사·표본·처방은 full 파일이 보유, 인덱스는 *고르게* 하는 한 줄). 줄이
  비대해지면(서사를 욱여넣으면) 인덱스가 다시 통독 대상으로 회귀하므로 **주기적으로 재압축**한다.
  압축 같은 대량·기계적 변환은 **서브에이전트 위임**으로 메인
  컨텍스트 증가를 막고, 키워드 배열·항목 수 보존은 **자가검증 말고 측정**으로 확인한다.
- `docs/`는 **인간 영역(참고 전용)**. 단, **스펙(`docs/spec/`)은 AI가 초안을 작성하고 인간이 검토**한다(인간 = 초안 도우미로 AI 활용). 그 외 docs는 명령 없이 수정하지 않는다.
- `tests/`에는 검증에 필요한 모든 파일을 둔다.
- `.dev/`에는 작업의 흔적을 기록한다 — 학습(learning), 문제 해결 과정(troubleshooting), 작업 로그·디버깅 히스토리·실험 결과·스크래치 패드.

### 2. Context — 맥락 이해·실수 교정·계층적 기록
- **이 단계의 목적은 두 가지다**: (1) 작업에 필요한 맥락을 *먼저 읽고 이해*한다, (2) 그 과정에서
  드러난 **기존 실수·잘못된 가정·오래된 기록을 바로잡는다**. 단순히 새로 적기만 하는 단계가 아니다.
- 작업 전에 관련 컨텍스트(상위→하위 `CLAUDE.md`, 스펙, learning/retrospect, 코드)를 확인해
  **현재 상태와 과거 결정·실수를 파악**한 뒤 진행한다. 어긋난 기록을 발견하면 그 자리에서 고친다.
- **`참고 자산:` 줄 필수** — 비자명한 작업은 시작할 때 **먼저 해당 `INDEX.md`를 읽는다**
  (`.dev/learning/INDEX.md`·`.dev/retrospect/INDEX.md`·`docs/spec/INDEX.md` — 각 항목 한 줄 후크+키워드).
  작업 키워드로 인덱스에서 관련 항목을 고르고, **한 줄 후크로 충분하면 그 줄을 근거로, 불충분할 때만
  그 항목의 full 파일을 연다**(인덱스에 없는 새 grep이 필요하면 병행하되 full 통독은 후크가 불충분한
  항목에 한정). Context 단계 응답에 `참고 자산: [번호-제목 → 적용점]`을 반드시 적는다. 관련 없으면
  `참고 자산: 관련 없음(사유)`를 명시한다. **이 줄이 없으면 Context를 스킵한 것으로 간주**한다.
  (회고는 다음 작업 Context에서 상기·적용될 때만 복리가 된다. 인덱스는 그 상기를 *싸게* 만든다 —
  통독 대신 한 줄. 새 spec/learning/retrospect를 만들면 같은 턴에 해당 INDEX.md에 한 줄을 추가한다.)
- **스코프를 활용**한다: User 영역 → Project 영역 → Folder 영역. **하위가 상위를 덮어쓴다**(위로 갈수록 추상적).
- 한꺼번에 다 적지 않는다. "이런 상황에서는 이걸 참고해"라고 안내하여 **필요한 것만 읽게** 한다.
- 폴더별 맥락은 해당 폴더의 컨텍스트 파일(예: 하위 `CLAUDE.md` 또는 README)에 둔다.

### 3. Planning — 계획 → 검토 → 승인 → 실행
- AI가 계획을 작성하고, 사람이 검토·피드백, 수정 후 **승인**받는다. 그 다음 실행한다.
- 무엇을 할지 모호하면 **의도가 잡힐 때까지 질문**하여 요구사항/태스크를 도출한다.
- 작성된 플랜은 반드시 **파일로 남긴다** → `docs/spec/NNN-{name}.md`.

### 4. Execution — 플랜 실행
- 중요한 기능은 **테스트로 동작을 먼저 확인**한다.
- 실행 방법을 영리하게 고민한다. 독립 작업은 **병렬 실행**을 적극 고려한다.
- 명확한 완료 기준이 있으면 **Ralph Loop**(완료 조건까지 반복)도 고려한다.

### 5. Verification — 결과 검증
- 해당 작업의 **완료 조건을 상기**한 뒤 검증한다.
- **자가검증은 최대한 피한다.** 가능하면 서브에이전트나 타사 모델(codex, copilot 등)로 **비판적 검증**을 받는다.
- 화면 검증이 필요하면 브라우저 제어/MCP/스크린샷 등 다양한 수단을 고민한다.
- 사이드이펙트가 예상되면 **dry-run**을 적극 고려한다.
- 사람 의존 검증은 지양한다. **반복적이고 빠른 검증**이 우선이다.

### 6. Compounding — 회고·자산화
- 무엇이 잘못됐고 무엇을 배웠는지 기록한다 → `.dev/learning/`, `.dev/troubleshooting/`.
- **AI 자신의 회고는 `.dev/retrospect/NNN-{name}.md`** (AI 영역). 인간이 작성하는 회고만 `docs/retrospect/`(인간 영역, 명령 필요).
- **새 자산을 만들면 같은 턴에 해당 `INDEX.md`에 한 줄을 추가한다**(learning/retrospect/spec). 한 줄
  = `NNN 제목 — 후크 [키워드]`. 인덱스가 Context 단계 읽기 비용을 낮추므로 갱신 누락은 그 복리를 깬다.
- **회고를 마치면 그 스펙 커밋까지가 한 단위다.** 같은 턴에 그 스펙 파일만 stage해 per-spec 메시지로
  커밋한다(매번 묻지 않는 **상시 승인**). **푸시·머지는 옵션** — 사용자가 명시할 때만, 절대 자동 금지.
  (스펙 단위로 그때그때 커밋해야 `seed.py`처럼 변경이 엉켜 나중에 커밋 가르는 곡예가 안 생긴다.)
- 같은 작업을 반복하면 **스킬/커맨드**로 도출한다.
- 같은 실수를 반복하면 Scaffolding에서 **훅이나 장치**로 막는다.
- 이 과정을 반복하며 절차를 선명하게, 실수를 적게, 사용자 의도에 가깝게 만든다.

---

## 폴더 구조

```
.claude/                        클로드 설정 파일
.dev/                           (AI 영역 — 작업의 흔적)
  learning/                     배운 것
  troubleshooting/              문제 해결 과정
  retrospect/  NNN-{name}.md     AI 회고
docs/                           (인간 영역, 참고 전용 — 명령 없이 수정 금지)
  spec/        NNN-{name}.md     스펙·플랜
  adr/         NNN-{name}.md     아키텍처 결정 기록
  retrospect/  NNN-{name}.md     인간 회고
tests/                          검증 자산
CLAUDE.md                       루트 컨텍스트(본 문서)
```

문서 파일은 `001-`처럼 **3자리 일련번호 + 케밥케이스** 이름을 쓴다.

---

## 에이전트 협업 (오케스트레이션)

메인 세션(AI)은 **오케스트레이터**다 — **계획·분해·종합**을 맡고, 실행은 성격에 따라
서브에이전트(`.claude/agents/`)에 위임한다. 목적 둘: (1) 메인 컨텍스트 절약(긴 작업이 컨텍스트를
잡아먹는 문제), (2) 모델 다양화(작업 성격에 맞는 모델 배치). **6단계 루프는 그대로** — 위임은 각 단계
*안에서의* 실행 방식이다.

- **deep-reasoner** (opus, 읽기 전용): **추론이 많이 필요한 일** — 어려운 버그 근인 분석, 설계
  트레이드오프, 아키텍처 결정 검토, 스펙·계획 적대 검토. 분석·보고만(수정 없음).
- **fast-worker** (sonnet, 수정 가능): **기계적 작업** — 대량 치환/rename, INDEX 재압축, 보일러플레이트,
  반복 패턴 적용. 전후 카운트 측정으로 마감(자가선언 금지).
- **메인이 직접**: 계획 수립, 작업 분해, 위임 결과 검증·종합, 사용자 소통, 커밋.
- 위임 시 과제는 **자기완결로** 넘긴다(필요 컨텍스트·완료 기준·출력 형식 포함) — 서브에이전트는 이
  대화를 못 본다. 결과는 메인이 검증 후 종합한다.

## 빠른 합의 규칙

- 매 응답 머리에 **현재 단계 `[단계 N — 이름]` 표기**.
- Context 단계엔 **`참고 자산:` 줄 필수** — **`INDEX.md` 먼저 읽고**(learning/retrospect/spec), 후크가 불충분할 때만 full 열기. 적용점 명시. 없으면 스킵.
- **컨텍스트 최적화는 Scaffolding 단계**에서 — `INDEX.md` 줄은 한 줄 후크 유지(비대해지면 재압축), 대량 변환은 서브에이전트 위임·보존은 측정으로 확인.
- `docs/`는 **인간 영역** — 스펙은 AI 초안 + 인간 검토, 나머지는 명령 없이 수정 금지.
- 모든 플랜은 **파일로** 남긴다.
- 단계 스킵은 **합의 + 사유** 필요.
- 검증은 **타자(서브에이전트·타사 모델)** 우선, 자가검증 지양.
- **메인=오케스트레이터**(계획·분해·종합): 깊은 추론→`deep-reasoner`, 기계 작업→`fast-worker` 위임(컨텍스트 절약·모델 적재적소).
- **"다음 할 일?"은 Scaffolding** — 단, 직전 작업의 회고가 미진하면 **회고 먼저** 하고 넘어간다.
- **회고 끝나면 그 스펙 커밋까지** 한 단위(상시 승인, per-spec). **푸시·머지는 사용자 명시 시만**(자동 금지).
