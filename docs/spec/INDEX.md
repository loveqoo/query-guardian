# Spec INDEX

- 001 MVP 범위·아키텍처 — MySQL+Druid 파서+재귀 IR+fail-closed 룰 계약(§6)+룰 4종+CodeMirror+저장 게이트; SELECT만 저장, 관리형 purpose; M1~M4 [MVP, 아키텍처, IR, Druid, 룰 계약, fail-closed, CodeMirror] (v2 승인)
- 002 제약 카탈로그 개편 — 컬럼 클래스 6종×kind 6종 사전→컬럼 매핑→룰 어휘 통제(디자인 1단계); 판정만(재작성 유보), IR 컬럼 참조 수집 필수(C1)+block 판정+복합 파티션+LIMIT 상한; 결정 7건 기록 [제약, 카탈로그, kind, PII, 클래스, columnRefs, 디자인] (v2 승인)
- 003 디자인 UI 전면 컨버팅 — dc.html이 유일 원본, 기존 UI 제거+셸(4그룹 7메뉴)+화면 7종 antd 컨버팅; A(실 로직 3)·B(부분 1)·C(스텁 3) 등급, 스텁=디자인 샘플 데이터+조용한 무시 금지, 백엔드 무변경 [디자인, 컨버팅, 셸, 스텁, antd] (구현 완료)
- 004 규칙 데이터화 — 사용자 정의 규칙(scope·AND/OR 트리·조건 단위 severity)+엔진, 조건 op requires·blocks·joins 판정(within·masked는 등록만+미판정 배지), IR 조인 등식 확장, RulesPage 실 연결; 하이브리드(시스템 룰 유지) [규칙, 엔진, op, 트리, severity, joinEqualities, 어휘 통제] (구현 완료)
- 005 승인 요청·쿼리 검토 워크플로 — 승인 후 쿼리 작성(모델 교정); 요청(순차 승인·행위자 스텁·실 규칙 참조)+저장 게이트 확장(승인된 요청 필수·테이블 커버)+쿼리 검토 상태; 화면 3종 실 연결 [승인, 검토, 워크플로, 순차 승인, 게이트 확장, purposeCode 승계, 테이블 커버] (구현 완료)
- 006 테스트 부채 상환 — spec 004/005의 미검증 fail-closed 경로 6종(판정불가 requires·손상 규칙 격리·dangling·동시 승인·규칙 드리프트·검토 재lint)을 회귀로 고정; 프로덕션 변경 0, 102건 통과 [테스트, 부채, fail-closed, 동시성] (완료)
- 007 인증(세션)·접근 권한 — actor 스텁→세션 principal(헤더 400 거부), 역할 3종 매트릭스, DB/테이블 allow를 lint·저장·요청 생성 전 경로 강제; 승인자는 app_user 승계, 권한 변경 비소급 [인증, 세션, 역할, 권한 게이트, actor 폐기, 판정 무권한 불변식] (구현 완료)
- 008 쿼리 실행·마스킹 재작성 — IR→SQL 재작성(MASK 치환·FILTER/LIMIT 주입, 전 스코프·fail-closed)+실행(동일 DB 데모 데이터, 읽기 전용·행 상한·타임아웃)+검토 승인 게이트+실행 감사; 실행 대상은 저장·승인된 쿼리 [실행, 마스킹, 재작성, 접수 검사, 실행 격리, 완전 재판정, 감사 도달성, sql_mode 금지목록] (v3 — M0~M2 완료 §2.8·§3.6·§3.8, 검증 3라운드 반영; M3 미착수) ⚠️ **§0 정정(2026-07-27)**: "자동 재작성"은 사용자 결정이 아니라 **AI가 디자인에서 유추**한 것을 사용자 결정으로 기록한 것 — 실제 모델은 **판정하고 제안한다**(서버가 사용자 SQL을 바꾸지 않음). 실행 모델 변경은 spec 012, IR 재검토는 ADR 001
- 010 실행 게이트 재설계 — 사용자 코드리뷰 R1~R6 대응: 게이트 순서를 **단계 증거**로(본체는 필드가 아니라 **발급 권한 폐쇄**), 실패는 **값**(`sealed GateStop`+조합자 하나, 예외 비지역반환 금지 — 롤백 side-effect가 호출자 트랜잭션에 남는다), 게이트는 트랜잭션 미개방·감사만 REQUIRES_NEW, 반출 있는 종결(SUCCESS·**PREVIEW**)은 기록이 선행조건·없는 종결은 best-effort+경보, 실행은 단회성 증거 토큰·실행기는 게이트 밖 호출 불가, append-only는 DB 권한, 파서는 자원 4축 유계(잔존 작업 포함), OCP는 변하는 축만 개방+기대치는 **닫힌 전역 정책**이 산출; arrow 미도입(재도입 조건 명시); P0(안전망)~P3 [재설계, 단계 증거, 값 기반 실패, 감사 등급, OCP, 증거 토큰, 자원 유계, 굿하트 방지] (v3 승인 — **P0·P1 완료**: 감사 코드 enum 21종+전수 검증(A0)·값 기반 실패(GateStop)+단계 단위·사본 3벌→1벌·감사 두 등급(A5)·응답 코드 단일 출처(A2)·예외 경계 고정(A3); 검토 2채널 21건 반영. **P1.5 완료**(파서 재귀 유계 A7 — Druid 어댑터로 고리 2/3 봉쇄·이진 연쇄 텍스트 상한·명시 스택·유계 풀; §4.7 전제 2개가 실측으로 뒤집혀 정정); **P2 완료**(발급 권한 폐쇄 — `sealed`+파일 private 구현체+**전용 패키지 `query.gate`**로 위조를 컴파일 오류화, `Judged<out Prior>`로 순서 강제(A1·A4), 증거 토큰 대신 구조 폐쇄로 **I9·A6 개정**(§4.2-1, 토큰 전환 조건 명시), 실행기 접근 1곳(I10·D-E 해소); 검토가 제약 부풀림·제네릭 소거 사각 2건 반증). **P3 완료**(enum을 영속 경계에서 enum으로·상수 이름과 **와이어 표현 분리**(`@JsonValue`+명시 `@JsonCreator`)·`privileged: Boolean` 29곳 → 발급이 폐쇄된 `Viewer`(세션에서만 발급, 이름을 열람 스코프로 좁혀 결정 14 보호)·`norm` 정의역 분할과 `parsePredicate` 타입 실패로 `!!` 36→23, A8; 부산 발견 셋 — 파생 프로퍼티가 `tree_json`에 저장돼 남의 프레임워크 기본값 하나가 fail-open을 막고 있었음, 등록 검증의 파싱 검사가 죽은 코드(fail-closed 검사가 앞에 있어 문법 오류가 다른 이유로 거절), **미결 정책**: STEWARD의 타인 쿼리 삭제는 열람 능력으로 통과 — 현행 고정 후 결정 대기))
- 009 모바일 레이아웃 대응 — 992px(lg) 미만 서랍 내비+내용 높이 전환+표는 가로 스크롤 격리; 데스크톱 픽셀 불변을 Playwright 스냅샷으로 증명(4뷰포트 32검사), 지표는 잘림·눌림 둘 다 측정 [모바일, 반응형, 시각 회귀, 디자인 충실도] (구현 완료)
- 011 의도 동치 판정 — 요건 룰을 **모양이 아니라 사실로**(파티션: 등호/열거/상·하한 쌍); **이 스펙은 완화**라 틀리면 유출 → 원칙 셋(사실은 넓히되 기준은 좁힌다·완화마다 악용 형태를 **먼저**·확신 없으면 완화 안 함), 불변식 5(특히 **OR는 모든 분기가 고정할 때만**), 겹 잇기는 네 조건 전부일 때만; Q0 안전망→Q1 경계 쌍→Q2 OR 분배(**사용자 결정: 파티션·술어 둘 다**)→Q3 겹 잇기(미착수, ADR 001에 따르면 Calcite에서는 공짜) [의도 동치, 완화, 안전망 선행, OR 분배, 겹 경계]
